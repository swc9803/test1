<template>
  d
</template>

<script>
import { onMounted } from 'vue'
// import { gsap } from 'gsap'
// import { ScrollTrigger } from 'gsap/ScrollTrigger'

export default {
  setup () {
    onMounted(() => {
      // const Spring = gsap.timeline()
      // ScrollTrigger.create({
      //   animation: Spring,
      //   trigger: '.scrollElement',
      //   start: '2% top',
      //   end: '15% 100%',
      //   scrub: 3
      // })
      // gsap.fromTo('#birds', { })
      // gsap.to('#bird1', { })
      // gsap.to('#bird2', { })
      // gsap.to('#bird3', { })
    })
  }
}
</script>

<style scoped>

</style>
